{% extends "block.html.twig" %}

{% block content %}

  <div class="logo-content">
  {% if site_logo %}
    <a href="{{ path('<front>') }}" title="{{ 'Home'|t }}" rel="home">
      <img src="{{ site_logo }}" alt="{{ 'Home'|t }}" class="img-fluid steve"/>
    </a>
  {% endif %}
  </div>
{% endblock %}
